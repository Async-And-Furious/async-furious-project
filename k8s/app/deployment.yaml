apiVersion: apps/v1
kind: Deployment
metadata:
  name: async-furious-api
  namespace: async-furious
spec:
  replicas: ${app_replicas}
  selector:
    matchLabels:
      app: async-furious-api
  template:
    metadata:
      labels:
        app: async-furious-api
    spec:
      automountServiceAccountToken: false
      containers:
        - name: api
          image: ${app_image}
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
              ephemeral-storage: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
              ephemeral-storage: "512Mi"
          envFrom:
            - configMapRef:
                name: async-furious-config
            - secretRef:
                name: async-furious-secret
          readinessProbe:
            httpGet:
              path: /api/v1/health/ready
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /api/v1/health/live
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /api/v1/health/live
              port: 3000
            failureThreshold: 30
            periodSeconds: 5
